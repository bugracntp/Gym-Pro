import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import GymManagementApp from './components/GymManagementApp';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <GymManagementApp />
  </React.StrictMode>
); 